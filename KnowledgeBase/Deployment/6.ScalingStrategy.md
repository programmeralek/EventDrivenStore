## 6. Scaling Strategy

Scaling is handled **per service**, not per system.

Examples:
- Orders Service can scale to handle high write throughput
- Inventory Service can scale independently for stock checks
- Billing Service can scale based on payment load

Kafka enables:
- Fan-out communication
- Partitioned ordering by key
- Horizontal consumer scaling via consumer groups

No code changes are required to scale services horizontally.
